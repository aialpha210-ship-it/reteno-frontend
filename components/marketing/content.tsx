import Link from "next/link";
import { knowledgeItems } from "@/lib/mock-data";

export function SectionIntro({eyebrow,title,copy}:{eyebrow?:string;title:string;copy?:string}) { return <div className="max-w-2xl"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">{eyebrow}</p><h2 className="mt-3 font-display text-4xl leading-tight text-ink sm:text-5xl">{title}</h2>{copy&&<p className="mt-4 text-lg leading-8 text-muted">{copy}</p>}</div> }
export function KnowledgePreview() { const item=knowledgeItems[3]; return <article className="rounded-3xl border border-line bg-white p-6 shadow-[0_16px_50px_rgba(24,26,27,.07)]"><div className="flex items-center justify-between"><span className="rounded-full bg-red-50 px-3 py-1 text-xs font-medium text-red-700">YouTube</span><span className="text-xs text-muted">8 min overview</span></div><h3 className="mt-5 font-display text-3xl leading-tight">{item.title}</h3><p className="mt-3 text-sm leading-6 text-muted">{item.summary}</p><div className="mt-6 border-t border-line pt-5"><p className="text-xs font-semibold uppercase tracking-wider text-muted">Key ideas</p><ol className="mt-3 space-y-2 text-sm leading-6"><li>1. Money grows on both the original principal and accumulated interest.</li><li>2. Time is often more important than the initial amount invested.</li><li>3. Consistency creates exponential growth.</li></ol></div><div className="mt-5 flex flex-wrap gap-2">{item.concepts.map(x=><span key={x} className="rounded-full bg-paper px-3 py-1 text-xs text-muted">{x}</span>)}</div></article> }
export function Cta({title,copy}:{title:string;copy:string}) { return <section className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><div className="rounded-3xl bg-ink px-7 py-12 text-center text-white sm:px-16"><h2 className="font-display text-4xl sm:text-5xl">{title}</h2><p className="mx-auto mt-4 max-w-xl text-white/70">{copy}</p><Link href="/get-started" className="mt-7 inline-flex rounded-full bg-white px-5 py-3 text-sm font-medium text-ink">Get Started</Link></div></section> }
