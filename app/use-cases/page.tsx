import { Cta, KnowledgePreview, SectionIntro } from "@/components/marketing/content";
import { MarketingLayout } from "@/components/marketing/site-chrome";
import { useCases } from "@/lib/mock-data";
export const metadata={title:"Use Cases | Reteno",description:"A personal learning system for people who learn from the internet."};
export default function UseCases(){return <MarketingLayout><section className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><SectionIntro eyebrow="Use cases" title="Built for people who learn from the internet." copy="From a lecture you need to revisit to an idea worth carrying into your next project, Reteno keeps the useful bits close."/><div className="mt-16 space-y-16">{useCases.map(([name,copy],i)=><article key={name} className="grid gap-8 border-t border-line pt-8 lg:grid-cols-[1fr_.7fr]"><div><p className="text-sm text-accent">0{i+1}</p><h2 className="mt-2 font-display text-4xl">{name}</h2><p className="mt-4 max-w-xl text-lg leading-8 text-muted">{copy}</p><div className="mt-7 grid gap-4 sm:grid-cols-2"><div><p className="text-sm font-medium">The moment</p><p className="mt-1 text-sm leading-6 text-muted">You find a video with a useful explanation, method, or perspective—then need it again later.</p></div><div><p className="text-sm font-medium">A simple workflow</p><p className="mt-1 text-sm leading-6 text-muted">Send it from the app you are in, then revisit the structured card in your library.</p></div></div></div><KnowledgePreview/></article>)}</div></section><Cta title="Give your curiosity somewhere to go." copy="Start forming a library from the things that already make you pause."/></MarketingLayout>}
