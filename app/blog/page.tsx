import { MarketingLayout } from "@/components/marketing/site-chrome";
import { SectionIntro } from "@/components/marketing/content";
import { blogPosts } from "@/lib/mock-data";
export const metadata={title:"Blog | Reteno",description:"Thoughts on building a personal learning practice."};
export default function Blog(){return <MarketingLayout><section className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><SectionIntro eyebrow="Reteno journal" title="A more intentional way to learn online." copy="Notes on curiosity, remembering, and building a personal library from what you already discover."/><div className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{blogPosts.map(([category,title,desc,time,date],i)=><article key={title} className="group overflow-hidden rounded-2xl border border-line bg-white"><div className={`h-44 bg-gradient-to-br ${["from-amber-100 to-orange-50","from-sky-100 to-indigo-50","from-rose-100 to-pink-50","from-lime-100 to-emerald-50","from-violet-100 to-purple-50"][i]}`}/><div className="p-6"><p className="text-xs font-semibold uppercase tracking-wider text-accent">{category}</p><h2 className="mt-3 font-display text-3xl leading-tight group-hover:underline">{title}</h2><p className="mt-3 text-sm leading-6 text-muted">{desc}</p><p className="mt-6 text-xs text-muted">{time} · {date}</p></div></article>)}</div></section></MarketingLayout>}
